{
  "name": "cressential",
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend && npm install && npm start",
    "heroku-postbuild": "cd truffle && npm install && cd ../client && npm install && npm run build "
  },
  "author": "Stonecap",
  "engines": {
    "node": "18.12.1"
  }
 }