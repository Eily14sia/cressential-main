{
  "name": "cressential",
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend && npm install && npm start",
    "deploy": "npm run install:client && npm run install:backend"
  },
  "author": "Stonecap"
 }