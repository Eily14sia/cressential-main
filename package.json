{
  "name": "cressential",
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend && npm install && npm start",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "author": "Stonecap"
 }